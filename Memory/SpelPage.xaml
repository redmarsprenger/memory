<Page x:Class="Memory.SpelPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Memory"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SpelPage" Width="775" Height="410" MinWidth="775" MinHeight="410">
    <Grid Background="White" Width="775" Height="410" MinWidth="775" MinHeight="410">
        <!--  here is the button that will deliver you to instellingenwindow. it makes uses of the style roundercorners  -->
        <Button x:Name="BacktoStartbtn" Content="Terug" Margin="32,31,637,315" Style="{StaticResource ResourceKey=roundercorners}" FontFamily="Calibri" FontSize="15"  Background="White" Click="BacktoStartbtn_Click"/>
        <!--  here is the button that will deliver you to the spelSelectiewindow. it makes uses of the style roundcorner  -->
        <Button x:Name="SpelSelectiebtn" Content="Nieuw Spel" Margin="317,154,306,192" Style="{StaticResource ResourceKey=RoundCorner}" FontFamily="Calibri" FontSize="20" RenderTransformOrigin="0.501,0.336"  Background="White" Click="SpelSelectiebtn_Click" />
        <!--  this is going to be a selection button it will make you be able to select your previous games. it makes uses of the style roundcorner  -->
        <Button x:Name="SpelSelectFolderBtn" Content="Laad Spel" Margin="317,285,306,61" Style="{StaticResource ResourceKey=RoundCorner}" FontFamily="Calibri" FontSize="20" RenderTransformOrigin="0.501,0.336" Background="White" Click="SpelSelectFolderBtn_Click"/>
    </Grid>
</Page>
